const { VisualResolver } = require('./VisualResolver');
const { EpisodeResolver } = require('./EpisodeResolver');
const { SearchResolver } = require('./SearchResolver');
const { RecommendationResolver } = require('./RecommendationResolver');
const { BannerResolver } = require('./BannerResolver');

module.exports = {
  VisualResolver,
  EpisodeResolver,
  SearchResolver,
  RecommendationResolver,
  BannerResolver,
};
